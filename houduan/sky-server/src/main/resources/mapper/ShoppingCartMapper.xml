<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
    <insert id="addShoppingCart">
            insert into shopping_cart (name, user_id, dish_id, setmeal_id, dish_flavor, number, amount, image,create_time)
            values
                (#{name}, #{userId}, #{dishId}, #{setmealId}, #{dishFlavor}, #{number}, #{amount}, #{image},#{createTime})
    </insert>
    <update id="updateNumberById">
        update shopping_cart set number=#{number} where id=#{id}
    </update>
    <delete id="deleteById">
        delete from shopping_cart where id=#{id}
    </delete>

    <select id="selectByUserIdAndDishId" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        where user_id=#{userId} and dish_id=#{dishId}
    </select>
    <select id="selectByUserIdAndSetmealId" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        where user_id=#{userId} and setmeal_id=#{setmealId}
    </select>
</mapper>
